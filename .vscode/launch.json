{
    "version": "0.2.0",
    "configurations": [
        {
            "sourceMaps": true,
            "file": "${workspaceRoot}/index.html",
            "type": "chrome",
            "request": "launch",
            "name": "Launch index.html",
            // "webRoot": "${workspaceRoot}"
        }
    ]
}